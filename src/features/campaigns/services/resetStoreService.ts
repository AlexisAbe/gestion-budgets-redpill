
// Simple service to reset the store
export const resetStoreService = async () => {
  // No backend interaction needed, this is purely client-side
  console.log('Campaign store reset requested');
  return true;
};
